spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/moviedb
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
project:
  poster: /path/to/poster/directory
base:
  url: "http://localhost:8080"